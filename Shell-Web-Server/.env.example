# 数据库配置
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=your_password
DB_NAME=shell_protector

# APK 签名 keystore
KEYSTORE_PATH=/path/to/your/shell.jks
KEYSTORE_PASS=your_keystore_password
KEY_ALIAS=shell

# JWT（留空则每次启动随机生成）
JWT_SECRET=
